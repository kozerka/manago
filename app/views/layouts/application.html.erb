<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Manago" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>


    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link href="https://cdn.jsdelivr.net/npm/@pinecss/pine@latest/css/pine.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <%= render 'shared/header' %>

    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <div id="flash">
        <% if flash[:notice] %>
          <div class="bg-green-50 border-l-4 border-green-400 p-4 my-4" role="alert">
            <p class="text-sm text-green-700"><%= flash[:notice] %></p>
          </div>
        <% end %>

        <% if flash[:error] || flash[:alert] %>
          <div class="bg-red-50 border-l-4 border-red-400 p-4 my-4" role="alert">
            <p class="text-sm text-red-700"><%= flash[:error] || flash[:alert] %></p>
          </div>
        <% end %>
      </div>

      <%= yield %>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
